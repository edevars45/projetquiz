

//entrainement//
let motsentr = prompt("Bonjour,veuillez entrez le maximum de mots le plus rapidement, pour testez votre rapidité !   Le 1er mots est : Aimer" )
console.log(motsentr)


const motappli ="Bonjour"
let motutilisateur = prompt("Entrez le mot :" + motappli)

console.log()


if (motutilisateur === motappli) {
    console.log("Bravo !")
} else {
    if (motutilisateur === "Greddin") {
        console.log("Restez correct !")
    } else {
        if (motutilisateur === "Mécréant") {
            console.log("Restez correct !")
        } else {


            
            if (motutilisateur === "Vilain") {
                console.log("Soyez gentil !")


            } else {
        
                console.log("Vous avez fait une erreur de frappe.")
            }
        }
    }
 

}
//commencement serieusement//

const listemot = ["Cachalot", "Pétunia", "Serviette", "Voiture","Pomme", 
"Chien","Chat", "Fleur", "Maison", "Livre", "Étoile","Plage", "Montagne",
"Café", "Amour", "Musique", "Bonheur", "Liberté", "Soleil", "Rêve","École", 
"Nature", "Art", "Étoile filante", "Arc-en-ciel", "Océan","Amitié", "Chocolat",
 "Bonjour", "Parfum", "Aventure" , "Feuille", "Étoile de mer","Bibliothèque",
  "Château", "Coucher de soleil", "Équilibre", "Forêt", "Glace", "Harmonie",
  "Inspiration", "Jardin", "Lumière", "Mélodie", "Nuage", "Oiseau", "Poésie",
  "Rivière", "Sourire", "Théâtre", "Univers", "Voyageur"]
  //52 mot en tout/
  //condition dans 
let scrore = 0

let motentree =prompt("Entrez le mot : " + listemot[0])

if (motentree === listemot[0]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[2])

if (motentree === listemot[2]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[3])

if (motentree === listemot[3]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[4])

if (motentree === listemot[4]) {
    scrore++
}


motentree =prompt("Entrez le mot : " + listemot[5])

if (motentree === listemot[5]) {
    scrore++
}


motentree =prompt("Entrez le mot : " + listemot[6])

if (motentree === listemot[6]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[7])

if (motentree === listemot[7]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[8])

if (motentree === listemot[8]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[9])

if (motentree === listemot[9]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[10])

if (motentree === listemot[10]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[11])

if (motentree === listemot[11]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[12])

if (motentree === listemot[12]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[13])

if (motentree === listemot[13]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[14])

if (motentree === listemot[14]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[15])

if (motentree === listemot[15]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[16])

if (motentree === listemot[16]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[17])

if (motentree === listemot[17]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[18])

if (motentree === listemot[18]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[19])

if (motentree === listemot[19]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[20])

if (motentree === listemot[20]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[21])

if (motentree === listemot[21]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[22])

if (motentree === listemot[22]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[23])

if (motentree === listemot[23]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[24])

if (motentree === listemot[24]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[25])

if (motentree === listemot[25]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[26])

if (motentree === listemot[26]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[27])

if (motentree === listemot[27]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[28])

if (motentree === listemot[28]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[29])

if (motentree === listemot[29]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[30])

if (motentree === listemot[30]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[31])

if (motentree === listemot[31]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[32])

if (motentree === listemot[32]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[33])

if (motentree === listemot[33]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[34])

if (motentree === listemot[34]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[35])

if (motentree === listemot[35]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[36])

if (motentree === listemot[36]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[37])

if (motentree === listemot[37]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[38])

if (motentree === listemot[38]) {
    scrore++
}

motentree =prompt("Entrez le mot : " + listemot[39])

if (motentree === listemot[39]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[40])

if (motentree === listemot[40]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[41])

if (motentree === listemot[41]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[42])

if (motentree === listemot[42]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[43])

if (motentree === listemot[43]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[44])

if (motentree === listemot[44]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[45])

if (motentree === listemot[45]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[46])

if (motentree === listemot[46]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[47])

if (motentree === listemot[47]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[48])

if (motentree === listemot[48]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[49])

if (motentree === listemot[49]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[50])

if (motentree === listemot[50]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[51])

if (motentree === listemot[51]) {
    scrore++
}
motentree =prompt("Entrez le mot : " + listemot[52])

if (motentree === listemot[52]) {
    scrore++
}


console.log(scrore);

for(let compteur = 0; compteur<500 ;compteur + 1) {
//Opérations qui seront répétées en boucle//
//COMPTEUR VAUT : 3

}